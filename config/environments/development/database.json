{
  "defaultConnection": "default",
  "connections": {
    "default": {
      "connector": "strapi-hook-mongoose",
      "settings": {
        "uri": "mongodb+srv://cairofresh:Av9gqZ8fPVge83VU@cairofresh-uyone.mongodb.net/test?retryWrites=true&w=majority",
        "ssl": "${process.env.DATABASE_SSL || true}",
        "database": "${process.env.DATABASE_NAME || 'cairofresh'}"
      },
      "options": {
        "ssl": "${process.env.DATABASE_SSL || true}",
        "authenticationDatabase": "${process.env.DATABASE_AUTHENTICATION_DATABASE || ''}"
      }
    }
  }
}
